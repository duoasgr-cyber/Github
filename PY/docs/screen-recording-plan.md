# 投屏录制# 投屏录制功能开发计划

> 分支：`feature/screen-recording`
> 目标：在投屏窗口中实时操作设备的同时，将操作录制为工作流步骤（类似按键精灵的录制宏），停止后直接追加到当前工作流。

---

## 一、需求与决策

# 投屏录制功能开发计划

> 分支：`feature/screen-recording`
> 目标：在投屏窗口中实时操作设备的同时，将操作录制为工作流步骤（类似按键精灵的录制宏），停止后直接追加到当前工作流。

---

## 一、需求与决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 操作即被录制 | 无需显式"插入等待"按钮，操作间等待自动记录为 `wait_after` |
| 2 | 录制时实时注入设备 | 边操作边看到设备响应 |
| 3 | 低延迟注入（不降画质） | 不可接受 `adb input` 的延迟，启用 scrcpy 控制协议 |
| 4 | 录制结果直接追加到当前工作流 | 无需预览窗确认 |

**录制语义**：开启"投屏录制# 投屏录制功能开发计划

> 分支：`feature/screen-recording`
> 目标：在投屏窗口中实时操作设备的同时，将操作录制为工作流步骤（类似按键精灵的录制宏），停止后直接追加到当前工作流。

---

## 一、需求与决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 操作即被录制 | 无需显式"插入等待"按钮，操作间等待自动记录为 `wait_after` |
| 2 | 录制时实时注入设备 | 边操作边看到设备响应 |
| 3 | 低延迟注入（不降画质） | 不可接受 `adb input` 的延迟，启用 scrcpy 控制协议 |
| 4 | 录制结果直接追加到当前工作流 | 无需预览窗确认 |

**录制语义**：开启"投屏录制"后，用户在实时投屏窗口内的# 投屏录制功能开发计划

> 分支：`feature/screen-recording`
> 目标：在投屏窗口中实时操作设备的同时，将操作录制为工作流步骤（类似按键精灵的录制宏），停止后直接追加到当前工作流。

---

## 一、需求与决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 操作即被录制 | 无需显式"插入等待"按钮，操作间等待自动记录为 `wait_after` |
| 2 | 录制时实时注入设备 | 边操作边看到设备响应 |
| 3 | 低延迟注入（不降画质） | 不可接受 `adb input` 的延迟，启用 scrcpy 控制协议 |
| 4 | 录制结果直接追加到当前工作流 | 无需预览窗确认 |

**录制语义**：开启"投屏录制"后，用户在实时投屏窗口内的所有点击/滑动操作会①实时注入设备# 投屏录制功能开发计划

> 分支：`feature/screen-recording`
> 目标：在投屏窗口中实时操作设备的同时，将操作录制为工作流步骤（类似按键精灵的录制宏），停止后直接追加到当前工作流。

---

## 一、需求与决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 操作即被录制 | 无需显式"插入等待"按钮，操作间等待自动记录为 `wait_after` |
| 2 | 录制时实时注入设备 | 边操作边看到设备响应 |
| 3 | 低延迟注入（不降画质） | 不可接受 `adb input` 的延迟，启用 scrcpy 控制协议 |
| 4 | 录制结果直接追加到当前工作流 | 无需预览窗确认 |

**录制语义**：开启"投屏录制"后，用户在实时投屏窗口内的所有点击/滑动操作会①实时注入设备（设备随之响应）②同时被录制为