# ⚠️ Legacy Code — 非活跃代码

此目录包含 **v1.0** 版本的原始代码，在 v2.0 重构后已不再使用。

## 目录说明

- `*.py` — v1.0 的独立脚本
- `patches/` — v2.0 开发过程中的临时修复脚本

## ⚠️ 重要

- **不要修改此目录中的文件**
- **不要从此目录导入代码**
- 新功能请在 `core/` 和 `ui/` 中实现

此目录仅作历史参考用途。
